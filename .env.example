# Must equal the exact Playwright version in package-lock.json.
PLAYWRIGHT_VERSION=1.62.1

# Host defaults. Docker always uses /vault via compose.yaml.
BROWSER_VAULT_HOME=./data
BROWSER_VAULT_HEADLESS=1
BROWSER_VAULT_LOG_LEVEL=info
BROWSER_VAULT_ARTIFACTS=0
